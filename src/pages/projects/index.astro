---
import BaseLayout from '@layouts/BaseLayout.astro';
import App from '@components/App.svelte';
import { projects } from '@utils/getSortedPosts';
---

<BaseLayout>
  <App client:only="svelte" posts={projects} />
</BaseLayout>
