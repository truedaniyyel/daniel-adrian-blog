---
import BaseLayout from '@layouts/BaseLayout.astro';
import App from '@components/App.svelte';
import { posts } from '@utils/getSortedPosts';
---

<BaseLayout>
  <App client:only="svelte" {posts} />
</BaseLayout>
