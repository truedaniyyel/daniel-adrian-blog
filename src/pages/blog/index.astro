---
import BaseLayout from '@layouts/BaseLayout.astro';
import App from '@components/App.svelte';
import { blog } from '@utils/getSortedPosts';
---

<BaseLayout>
  <App client:only="svelte" posts={blog} />
</BaseLayout>
